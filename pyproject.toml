[tool.black]
line-length = 88

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E","F","I"]

[tool.ruff.lint.isort]
known-first-party = ["app"]

[tool.mypy]
ignore_missing_imports = true
[project]
dependencies = [
    "fastapi",
    "starlette",
    "httpx",
    # ...other dependencies
]
